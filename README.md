Instructions

How to Run

Local Development

Run the following command to start the Vite dev server:

git clone git@github.com:kalelroscoe812/petKennel.git

cd petKennel

npm install

npm run dev


Note: Vite defaults to port 5173. Check your terminal output for the local URL.
